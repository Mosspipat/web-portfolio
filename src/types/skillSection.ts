import { ReactNode } from "react";

export type SkillItemProps = {
  name: string;
  level: string;
  iconElement?: ReactNode;
};

export type ToolItemProps = {
  name: string;
  level: string;
  iconElement?: ReactNode;
};
